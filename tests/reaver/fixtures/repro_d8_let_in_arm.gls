external mod Reaver.BPLAN {
  add : Nat → Nat → Nat
  eq : Nat → Nat → Nat
}

let go : Nat → Nat → Nat
  = λ x y → match (BPLAN.eq x 0) {
      | 0 → let b = BPLAN.add x y in BPLAN.add b 1
      | _ → y
    }

-- Trace target: `go 5 10` should evaluate to `(5+10)+1 = 16`.
let main : Nat = go 5 10
